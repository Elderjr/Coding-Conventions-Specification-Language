<faultTypeRule>
  <ocl>
    IfStatement.allInstances()->select(ifStatement1 |
      --Scope Conditions
      let stmt2 : ASTNode = ifStatement1.thenStatement in (not stmt2.oclIsUndefined()) and
      stmt2.oclIsKindOf(Statement)
    )
    ->reject(node: ASTNode | node.originalCompilationUnit.oclIsUndefined())
    ->reject(node: ASTNode | node->closure(oclContainer())->exists(oclIsKindOf(Javadoc)))
  </ocl>
  <strategy name="All" custom="false" />
  <action name="Mia" custom="false" />
</faultTypeRule>
